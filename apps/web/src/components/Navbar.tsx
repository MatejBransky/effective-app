// Static for now - nothing real to show yet. Iteration 2+ wires this up to
// domain/shell state via the Effect runtime (apps/web/src/runtime/runtime.ts).
export function Navbar() {
  return (
    <nav className="navbar">
      <span className="navbar-brand">Effective</span>
    </nav>
  );
}
