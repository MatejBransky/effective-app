.DS_Store
node_modules
.turbo
*.log
dist
dev-dist
*.local
.env
.cache
routeTree.gen.ts
# apps/infrastructure's alchemy-effect build bundles + local state (Cloudflare.state()
# is the real state backend - see docs/data-model.md - this is just local scratch space).
.alchemy
